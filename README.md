# EQUAL LANDING PAGE

#### HOW TO RUN

Execute the following commnads:

#### Install dependencies

`npm install`

#### Build project

`npm run build`

#### Run project

`npm start`

A server will be listening on:

`localhost:3000`